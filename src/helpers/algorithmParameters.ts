export interface AlgorithmParameters {
  fontFamily: string;
  fontSize: number;
  maxWidth: number;
  maxHeight: number;
  maxFontSize: number;
  maxLineHeight: number;
  minFontSize: number;
  lineHeightRatio: number;
}
